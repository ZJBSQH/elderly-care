<?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.elderlycare.mapper.NotificationMapper">

    <select id="selectByUserId" resultType="com.elderlycare.pojo.entity.Notification">
        SELECT * FROM notification
        WHERE user_id = #{userId}
        ORDER BY send_time DESC
    </select>

    <select id="countUnread" resultType="java.lang.Long">
        SELECT COUNT(*) FROM notification
        WHERE user_id = #{userId}
        AND read_status = 0
    </select>

    <!-- 查询用户的预警通知 -->
    <select id="selectUserWarnings" resultType="com.elderlycare.pojo.entity.Notification">
        SELECT * FROM notification
        WHERE user_id = #{userId}
        AND elder_id IN
        <foreach collection="elderIds" item="elderId" open="(" separator="," close=")">
            #{elderId}
        </foreach>
        AND notify_type = 2
        ORDER BY send_time DESC
    </select>

    <!-- 统计未读预警数量 -->
    <select id="countUnreadWarnings" resultType="int">
        SELECT COUNT(*) FROM notification
        WHERE user_id = #{userId}
        AND elder_id IN
        <foreach collection="elderIds" item="elderId" open="(" separator="," close=")">
            #{elderId}
        </foreach>
        AND notify_type = 2
        AND read_status = 0
    </select>

    <!-- 标记所有预警为已读 -->
    <update id="markAllWarningsAsRead">
        UPDATE notification
        SET read_status = 1, read_time = NOW()
        WHERE user_id = #{userId}
        AND elder_id IN
        <foreach collection="elderIds" item="elderId" open="(" separator="," close=")">
            #{elderId}
        </foreach>
        AND notify_type = 2
        AND read_status = 0
    </update>

</mapper>
