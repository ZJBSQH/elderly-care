<?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.elderlycare.mapper.RemindTaskMapper">

    <select id="selectTodayTasks" resultType="com.elderlycare.pojo.entity.RemindTask">
        SELECT * FROM remind_task
        WHERE remind_date = #{today}
          AND status = 1
          AND (end_date IS NULL OR end_date >= #{today})
        ORDER BY remind_time ASC
    </select>

    <select id="selectByElderId" resultType="com.elderlycare.pojo.entity.RemindTask">
        SELECT * FROM remind_task
        WHERE elder_id = #{elderId}
        <if test="remindType != null">
            AND remind_type = #{remindType}
        </if>
        <if test="startDate != null and startDate != ''">
            AND remind_date &gt;= #{startDate}
        </if>
        <if test="endDate != null and endDate != ''">
            AND remind_date &lt;= #{endDate}
        </if>
        ORDER BY remind_time ASC
    </select>

</mapper>
