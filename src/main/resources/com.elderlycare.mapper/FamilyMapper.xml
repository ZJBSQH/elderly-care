<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.elderlycare.mapper.FamilyMapper">

    <!-- 查询家属绑定的所有老人 -->
    <select id="selectBoundEldersByFamilyUserId" resultType="com.elderlycare.pojo.entity.Elder">
        SELECT e.* FROM elder e
                            INNER JOIN family f ON e.id = f.elder_id
        WHERE f.family_user_id = #{familyUserId}
          AND f.bind_status = 1
    </select>

    <!-- 查询老人绑定的所有家属 -->
    <select id="selectBoundFamilyMembersByElderId" resultType="com.elderlycare.pojo.entity.User">
        SELECT u.* FROM user u
                            INNER JOIN family f ON u.id = f.family_user_id
        WHERE f.elder_id = #{elderId}
          AND f.bind_status = 1
    </select>
</mapper>

